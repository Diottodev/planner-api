import * as React from "react";
type EmailTemplateProps = {
    preview: string;
    name: string;
    email: string;
    url: string;
};
export default function email_auth({ name, preview, email, url, }: EmailTemplateProps): React.JSX.Element;
export {};
//# sourceMappingURL=email-auth.d.ts.map